%tr{:class=> cycle(:first, :second)}
  %td
    %a{:href => user_path(user)}
      = SecureRandom.hex(1).upcase
      = user.id
  %td= user.created_at.to_date
  %td= user.height
  %td= user.birth_date
  %td= user.education
  %td= user.city
  %td= user.occupation
